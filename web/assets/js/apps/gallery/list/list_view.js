// Generated by CoffeeScript 1.10.0
(function() {
  app.module('GalleryApp.List', function(List, app, Backbone, Marionette, $, _) {
    List.Layout = Marionette.LayoutView.extend({
      template: '#album-list-layout',
      regions: {
        albumsRegion: '#albums-region'
      }
    });
    List.Album = Marionette.ItemView.extend({
      template: '#album-list-item',
      events: {
        'click a.show_album > button': 'showAlbum'
      },
      showAlbum: function(e) {
        app.trigger('album:show', this.model.id);
        e.preventDefault();
      }
    });
    List.Albums = Marionette.CollectionView.extend({
      tagName: 'div',
      className: 'list-group',
      childView: List.Album
    });
  });

}).call(this);
